### PCA
$\|H\|<\infty$
we know that $\mathbb P[R(h)\le R_S(h) + \sqrt{\frac{\ln \|H\| + \ln \frac{2}{\delta}}{2m}}] \ge 1-\delta \forall h\in H, S=\{(x_i, c(x_i) )\}_{i=1}^m \sim P^m$

$R_S(h) = \frac 1m \sum_{i=1}^m \ell( h(x_i), c(x_i) )$

the goal is pick $h\in H$ based on $S$ to minimize $R_S(h) + \sqrt{\frac{\ln \|H\| + \ln \frac{2}{\delta}}{2m}}$

### PAC of $\|H\|=\infty$
$\mathbb P[\exists h|R_S(h) > \epsilon] = \mathbb P[\cup_{i=1}^{\|H\|} R_S(h_i) > \epsilon]$
$\le \sum_{i=1}^{\|H\|} \mathbb P(R_S(h_i) > \epsilon)$

- note that $\mathbb P(R_S(h_i) > \epsilon) \le \mathbb P( \|R_S(h_i) - E_S(R(h_i))\| > \epsilon)$

### PCA to express $R(h)$

#### Step 1. uniform bound
strategy: $\sup_{h\in H} (R(h) - R_S(h))\le [???]$

$\forall h\in H$, $R(h) - R_S(h) \le \underbrace{\sup_{h\in H} R(h) - R_S(h)}_{{\rm noted\ as\ f(S)}} \le [???]$

note that 
$S_1, S_2\sim P^m$
$f(S_1) - f(S_2) = \sup_{h_1\in H} (R(h_1) - R_{S_1}(h_1)) - \sup_{h_2\in H} (R(h_2) - R_{S_2}(h_2))  = \sup_{h_1} \left[ R(h_1) - R_{S_1}(h_1) - \sup_{h\in H} (R(h) - R_{S_2}(h))\right]$

$\le R(h_1) - R_{S_1}(h_1) - R(h_1) + R_{S_2}(h_1) = R_{S_2}(h_1) - R_{S_1}(h_1) \le \sup_{h\in H} [R_{S_2}(h) - R_{S_1}(h)]$

therefore, $\sup_{h\in H} [R_{S_2}(h) - R_{S_1}(h)]$ can be used as an uniform bound

###### Lemma: McDiarmid's inequality
- Let $X_1,X_2,...$ are independent Ranom variables
$f:X^n \to \mathbb R$
condition:
    - $\|f(x_1,...,x_m) - f(x_1,..., x_i',...,x_m)\| \le c_i$, $i$ is at any point in $(x_1,...x_m)\in X^n$

- then
$\mathbb P\left[ \|f(S) - \mathbb E(f(S))\| \ge \epsilon \right] \le 2\exp\left( -\frac{2\epsilon^2}{\sum_i c_i^2} \right)$


#### step 2. $S_1$ and $S_2$ differ only one sample
At this case, we can use McDiarmid's inequality to bound $f(S)$


<!-- #### step 3. symmetrization lemma -->
$\mathbb E_S [f(S)] = \mathbb E[\sup_{h\in H} (R(h) - R_S(h_))]$
$R(h) = \mathbb E_S[R_S(h)]$ by large of numbers theorem

We want to estimate $\mathbb E_S\left\{\sup_{h\in H} \mathbb E_{S'}[R_{S'}(h) - R_S(h)]\right\}$
using the fact $\sup \mathbb E [??] \le \mathbb E\sup[??]$, we have

$\mathbb E_S\left\{\sup_{h\in H} \mathbb E_{S'}[R_{S'}(h) - R_S(h)]\right\} \le \mathbb E_S\left\{ \mathbb E_{S'} \sup_{h\in H} [R_{S'}(h) - R_S(h)]\right\}$
$=\mathbb E_{S, S'}\left\{ \sup_{h\in H} [R_{S'}(h) - R_S(h)]\right\}$
- at here we define the reward(or loss) function $R_S(h) = \sum_i h(x_i)\times \sigma_i$ ($\sigma_i=c(x_i)\in \{0,1\}$ ), 
therefore $R_{S'}(h) - R_S(h) = \frac 1m \sum_{i=1}^m (h(x_i') - h(x_i))$, $h:x\to \{-1,1\}$

use Rademacher's variables inequality, we have
$\mathbb E_{S, S'}\left\{ \sup_{h\in H} [R_{S'}(h) - R_S(h)]\right\}$
$=\mathbb E_{S, S', \sigma}\left\{ \sup_{h\in H} [\sigma_i (h(x_i') - h(x_i))]\right\}$, where $\sigma_i\in \{-1, 1\}$ with equal probability

use $\sup (A-B) \le \sup A + \sup B$, we have

$\mathbb E_{S, S', \sigma} \left\{ \sup_{h\in H} [\sigma_i (h(x_i') - h(x_i))]\right\}$

$\le \underbrace{\mathbb E_{S, \sigma} \left\{ \sup_{h\in H} [\sigma_i h(x_i)]\right\}}_{R^\sigma_m(h)} + \mathbb E_{S', \sigma} \left\{ \sup_{h\in H} [\sigma_i h(x_i')]\right\} \le 2 R_m^{\sigma}(H)$

###### remark:
- given datas $\{x_i\}$, assume $\{\sigma_i\}\subseteq \{-1,1\}^m$ are the classes of each $x_i$, our goal will be choose $h\in H$ s.t. that best matched the datas, i.e. $\sum_i h(x_i)\times \sigma_i$ is high

note that the analysis does not use the size of $H$, therefore $\|H\|$ can be $\infty$ of this bound.

###### analyze the condition for McDiarmid's inequality
$G:\{g: X\to [0,c]\}$ ($g$ can be a regression function)

given $S = \{z_i\}_{i=1}^m, \bar g(S) = \frac 1m \sum_i g(z_i)$

$\mathbb E_D[g] = \mathbb E_S[\bar g(S)]$: expective loss

$\Phi(S) = \sup_{g\in G} [\mathbb E_D(g) - \bar g(S)]$

Let $S$ and $S'$ differ exactly one element at $i$,
$z_i$ in $S$ and $z_i'\in Ss$

$\Phi(S) - \Phi(S') \le \sup_{g\in G} (\bar g(S') - \bar g(S))\le \frac cm$

Therefore, $g$ ($h$ at this case) is satisfy the constraint of McDiarmid's inequality with $c_i = \frac{c}{m} = \frac 2m$

#### step 3.
$E_S(f(S))\le 2R^\sigma_m(H)$

Apply McDiarmid's inequality
$\mathbb P(\Phi(S) - \mathbb E[\Phi(S)] < \epsilon ) \ge 1-2\exp(-\frac{2m\epsilon^2}{c^2})$

$\implies$ take $1-2\exp(-\frac{2m\epsilon^2}{c^2}) = 1-\delta$, we obtain $\epsilon = c \sqrt{\frac{\log \frac 2\delta}{ 2m}}$

 